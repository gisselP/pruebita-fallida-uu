import { ResaltarCita } from './resaltar-cita';

describe('ResaltarCita', () => {
  it('should create an instance', () => {
    const directive = new ResaltarCita();
    expect(directive).toBeTruthy();
  });
});
